# busdk
BusDK superproject that bundles the `bus` dispatcher and all `bus-*` modules as Git submodules, pinning compatible versions and providing a single Makefile to build and install the full BusDK CLI toolchain from one command.
