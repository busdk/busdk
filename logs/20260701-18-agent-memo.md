# 2026-07-01 18:00 EEST Agent Memo

The operator corrected a stale naming mistake during the QEMU/WASM networking
handoff: there is no BusDK product called "browser lab." The actual surface is
the QEMU port for WebAssembly running Bus Engine OS as a `virtual-server`
profile. I checked the BusDK and `busdk.com` guidance first because this is a
public naming correction, and the existing guidance already says public names
must use durable product concepts instead of prototype labels.

The audit found that `bus-engine-os` already rejects the old `artifact
browser-lab` command as obsolete, while the website still had a transitional
`docs/engine/browser-lab` static app path, iframe source, CSS class, canonical
URL, generated bundle default path, and plan evidence. I renamed that website
surface to `wasm-virtual-server`, updated the static bundle script to use
`/engine/wasm-virtual-server/` by default, removed the legacy
`BUS_ENGINE_BROWSER_LAB_*` input fallbacks, and updated `busdk.com/PLAN.md` so
the live tracker records the correction.

I also added a compact BusDK guidance rule under public surface naming:
browser-hosted operating system work should be called the QEMU/WASM port,
Bus Engine OS, and the relevant `virtual-server` or `virtual-desktop` profile;
`browser lab` / `browser-lab` is allowed only when rejecting or migrating an
obsolete alias. The next useful check is to run the focused website/static
searches and any available `busdk.com` verification target before committing
the BusDK submodule update and promoting the supervisor pointer.
