## Session Context

The repos feature is back in implementation mode. The operator confirmed that
the Bus Workers infrastructure should be usable now, while preserving the
earlier constraint that the worker feature line itself should not be changed.
The target design remains catalog-first: `bus-integration-repos` owns the
Events-backed repository catalog and Git runtime behavior, `bus-api-provider-
repos` owns HTTP/controller surfaces later, and `bus-repos` owns the public
`bus repos` CLI later.

The local BusDK stack is currently healthy after the service-profile repairs
committed earlier in this session. `bus-services stack ps` reports Postgres,
Events, tasks, repos, repos SSH, workers, and API running from this checkout,
with Events on `127.0.0.1:18181`, API/workers on `127.0.0.1:18190`, and repos
SSH on `127.0.0.1:22222`.

## Work Narrative

I resumed by checking the supervisor root and BusDK worktrees, the running
service stack, and the replacement worker state. The supervisor root only shows
the BusDK submodule pointer changed, which is expected after the local BusDK
service-repair commits. Inside BusDK, `develop` is ahead of `origin/develop`
and clean.

The first App Server worker reached `running` but could not execute commands on
this Linux host because the Codex sandbox attempted a bubblewrap operation that
the host does not permit. That worker was stopped earlier. I therefore
continued with replacement worker
`58f243b1-9219-47bb-87e9-3a85081f58e8`, created with
`sandbox=danger-full-access`. Its status shows `running` and `ready`, with the
worker-owned product checkout under `.bus/services/workers/runtime/58f243b1-
9219-47bb-87e9-3a85081f58e8/product-worktree`.

I sent the worker a complete live task message rather than relying on any
side-channel prompt file. The task is deliberately narrow: implement catalog
CRUD Event handling in `bus-integration-repos` only, leave API and CLI modules
alone, do not touch `bus-integration-worker`, keep existing lifecycle/workspace
behavior compatible, and prove the change with focused tests, `go test ./...`,
and `git diff --check` in `bus-integration-repos`. I will not count the lane as
active implementation until the worker shows assistant output and either a
worker-owned diff or a concrete no-change/missing-contract diagnosis.

The worker produced assistant text and corrected its module materialization by
checking out the `bus-integration-repos` submodule in its own product worktree.
It then spent several messages on read-only discovery and path corrections, and
the worker-owned module stayed clean through multiple checkpoints. I sent a
tighter checkpoint message instructing it to stop broad cross-module discovery,
use `bus-integration-repos` as the owner of provisional catalog event constants
and payload DTOs for this slice, and produce a minimal first diff covering
catalog manager methods, processor event handling, registration/stream names,
and focused tests. The boundary remains unchanged: `bus-repos` may be read as
context but must not be edited, and `bus-integration-worker` remains out of
scope.

The nudge surfaced that the worker had started a partial patch but remained
path-confused and still had no clean reviewable result. I stopped worker
`58f243b1-9219-47bb-87e9-3a85081f58e8` to avoid concurrent edits and took a
supervised worker-owned recovery path in its product worktree. The incomplete
state already contained useful manager and processor scaffolding, but it did
not compile: catalog DTOs and event wiring were only partially present, some
validation calls pointed at the wrong package, and one attempted legacy remote
conversion would have produced invalid catalog remotes without URLs. I finished
the catalog Event surface in the worker-owned checkout, kept the new public
catalog payloads free of internal path fields, ran `go test ./...` and
`git diff --check`, then applied the accepted patch back to the primary
`bus-integration-repos` checkout.

The primary module now has commit `25cd8a0` (`Add repos catalog CRUD events`).
It adds Events-backed catalog group/repo/remote CRUD handling, command and host
registration listener names, manager persistence paths, focused manager and
processor tests, and a precise checked `PLAN.md` item for the completed slice.
Verification in the primary checkout passed with `go test ./...` and
`git diff --check` in `bus-integration-repos`, plus `go test ./...` in
`bus-integration` to catch combined registration compile issues.

## Current State

The implementation slice is accepted at the module level. BusDK still needs a
superproject commit pinning `bus-integration-repos` to `25cd8a0` and recording
this memo update. The worker that produced the partial patch has been stopped,
so there is no active worker process racing the primary checkout.

## Final State

The BusDK superproject is now committed at `cb103ee`, pinning
`bus-integration-repos` to `25cd8a0`, and the supervisor root is committed at
`054eb70` to record that BusDK pointer. The local service stack remains healthy.
The accepted slice gives `bus-integration-repos` the Events-owned catalog CRUD
core, but the overall repos feature is not complete yet.

The next slice should start with the shared/API contract rather than code
generation: `bus-repos` still lacks catalog DTOs/client methods for groups,
repos, and repo remotes, and `bus-api-provider-repos` still has an older
action-shaped HTTP surface. While reviewing that next path I also noticed that
slash-containing repo IDs such as `busdk/product` need deliberate API routing
or query/body-based IDs; a naive `/api/v1/repos/{id}` path will be awkward for
these IDs. The next implementation lane should therefore define the shared
catalog DTOs and HTTP route shape first, then wire provider tests that publish
the new `bus.repos.catalog.*` Events without running Git or writing catalog
state directly.
