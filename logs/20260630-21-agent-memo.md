# 2026-06-30 21 UTC Agent Memo

The current goal shifted from the completed QEMU/WASM graphics and keyboard
MVP into the next browser-hosted Engine OS product capability: Codex App Server running
inside the Bus Engine OS virtual server, reachable by frontend applications
and other WASM applications through a controlled browser-to-guest path.

I checked the relevant module guidance and plans before editing. QEMU's
`PLAN.md` had no open work because the previous graphics/keyboard MVP was
marked complete. That was correct for the old goal but wrong for the new one:
the next QEMU work is a generic service bridge, not more display work. I added
new active QEMU plan items for selecting a browser-to-guest channel, extending
the guest manifest, implementing a constrained browser API or iframe
`postMessage` surface, proving the path with a tiny generic guest service, and
documenting the downstream handoff. I left Bus Engine product behavior out of
QEMU.

In Bus Engine OS, I refined the active browser-hosted Engine OS goal so the image must
include Codex App Server as a governed in-guest service, not only as a packaged
binary. The added plan items cover the service boundary, a systemd unit or
equivalent policy-gated service, a guest adapter that exposes only constrained
health/status/request shapes over the bridge, `virtual-server` and
`virtual-desktop` smoke proofs, and documentation for frontend and WASM apps.
The package recipes for Codex CLI and Codex App Server already exist for
aarch64 and x86_64, but their license status remains pending until bundled
helper notices are reviewed.

I also updated the Bus Engine CLI, API provider, Engine integration, VM
integration, QEMU provider, and VM API provider plans so the system has a
coherent control surface. The product-facing layers should report stable
bridge and service-health states, preserve default single-instance behavior,
support named instances later, and avoid exposing QEMU transport details,
guest filesystem paths, raw port forwarding, arbitrary command execution, or
model credentials.

Verification so far is planning-level only. `git diff --check` passed in QEMU,
Bus Engine OS, `bus-engine`, `bus-api-provider-engine`,
`bus-integration-engine`, `bus-integration-vm`, `bus-integration-qemu`, and
`bus-api-provider-vm`. No implementation or runtime proof was attempted in this
phase.

Next: commit the plan updates in each repository, then record the updated
submodule pins in the BusDK superproject. The active goal is now the in-guest
Codex App Server browser-hosted Engine OS bridge.

The operator corrected the naming again: the old display name is no longer in
use. I updated active planning language to use browser-hosted Engine OS
wording, added the preferred `bus engine os artifact browser-hosted` command,
and kept the old artifact subcommand only as an obsolete compatibility alias
with a warning. Generated artifacts now use `browser-hosted-manifest.json`, and
the artifact package internals were renamed to `BrowserHosted...` symbols.

The operator also added frontend power buttons and suspend/resume as desired
capabilities. I wrote those into the relevant plans. QEMU owns generic browser
power-control and browser-state planning; Engine OS owns guest-side graceful
shutdown/reboot readiness and compatibility metadata; Bus Engine, Engine API,
Engine integration, VM integration, QEMU provider, and VM API modules own
stable public operations and provider-neutral power/saved-state handling.

Focused verification for the naming implementation passed with
`GOCACHE=/tmp/bus-engine-os-go-cache go test -mod=readonly -tags 'netgo,osusergo' ./internal/cli ./pkg/artifact`.
`git diff --check` also passed across the touched repositories.

I continued the goal by completing QEMU's next bridge-enabling PLAN item. The
generic browser-to-guest service bridge design is now documented in
`docs/devel/wasm-support-plan.rst` and the corresponding QEMU `PLAN.md` item
is checked. The selected MVP channel is browser `postMessage` or direct
harness JavaScript API on the outside, then worker messaging, then a generic
QEMU WebAssembly browser chardev attached to a dedicated virtio-console or
virtserialport device inside the guest. QMP remains for VM control, not guest
service methods. Browser networking remains explicitly out of scope for the
first bridge. The note records the security assumptions, origin validation,
bounded payloads, timeout handling, and the first tiny guest health-service
proof shape. Verification was `git diff --check` plus a retired-name search in
the QEMU plan/docs. The QEMU branch was committed as
`13d3b126ac Document browser service bridge design` and pushed to
`bus-engine-wasm-support-plan`.

I completed the next QEMU PLAN item for browser service bridge manifest
metadata. The guest manifest helper now accepts a generic `serviceBridge`
object with bridge kind, request and response channel names, readiness marker,
health request, timeout, maximum payload size, and interactive-only status.
The browser smoke runner preserves that metadata in the browser URL and result
JSON, and the browser page exposes it in `qemuWasmSmokeState` without claiming
that the bridge transport itself is implemented yet. The developer note now
documents the accepted metadata fields. Focused verification passed with
`node scripts/ci/wasm-guest-manifest-test.mjs`,
`node scripts/ci/wasm-browser-smoke-runner-test.mjs`, and `git diff --check`.
The QEMU branch was committed as
`4c023e925d Add browser service bridge manifest metadata` and pushed to
`bus-engine-wasm-support-plan`.

I completed the next QEMU PLAN item for generic browser service bridge
plumbing. QEMU now has an Emscripten-only `wasm` chardev backend in
`chardev/char-wasm.c`, QAPI wiring for `-chardev wasm,...`, and browser
harness generation of two virtio-serial ports for request and response JSONL
channels. The browser harness exposes `qemuWasmServiceBridge.request()` for
same-page callers and a same-origin `qemu-wasm-service-request` postMessage
wrapper for iframe callers. It correlates responses by id, applies timeouts,
rejects oversized request payloads, records counters/status in smoke state,
and avoids storing request or response payloads in result state by default.
Focused verification passed with `python3 scripts/qapi-gen.py -o
/tmp/qemu-qapi-check qapi/qapi-schema.json`,
`node scripts/ci/wasm-browser-smoke-args-test.mjs`,
`node scripts/ci/wasm-guest-manifest-test.mjs`,
`node scripts/ci/wasm-browser-smoke-runner-test.mjs`, and `git diff --check`.
This host does not have `emcc`, `meson`, or `ninja` on PATH, so a C/WebAssembly
compile proof was not run here. The QEMU branch was committed as
`26aa61a32e Add generic browser service bridge plumbing` and pushed to
`bus-engine-wasm-support-plan`.

I corrected the QEMU goal contract so it explicitly says to finish unchecked
`PLAN.md` items first, and only move highest-value work from `FUTURE_WORK.md`
into `PLAN.md` when the active plan is empty or blocked on a concrete external
dependency. I also added the same ordering rule to QEMU `AGENTS.md`.

I advanced the next QEMU PLAN item by adding a tiny generic service-bridge
guest fixture. `scripts/ci/wasm-build-smoke-initramfs.py
--service-bridge-smoke` now generates an initramfs that waits for the request
and response virtio ports, prints `QEMU_WASM_SERVICE_READY`, reads one JSONL
request, returns a structured health response, and then prints the normal
serial success marker. The browser harness now automatically sends the
manifest `healthRequest` once the bridge is ready and the QEMU module is
attached, while preserving interactive-only behavior for manual callers. The
developer note documents this proof fixture.

Focused verification passed with `python3
scripts/ci/wasm-build-smoke-initramfs-test.py`, `node
scripts/ci/wasm-browser-smoke-args-test.mjs`, `node
scripts/ci/wasm-guest-manifest-test.mjs`, `node
scripts/ci/wasm-browser-smoke-runner-test.mjs`, `python3 scripts/qapi-gen.py
-o /tmp/qemu-qapi-check qapi/qapi-schema.json`, a retired-name search for the
old Browser Lab wording, and `git diff --check`. A full Chrome/Chromium proof
of the new chardev bridge was not run on this host because there is no fresh
WebAssembly build artifact for the new chardev and no Chrome/Chromium binary
available here. The QEMU branch was committed as `6038b95fe9 Add generic
service bridge smoke fixture` and pushed to `bus-engine-wasm-support-plan`.
