id: postgres/managed
aliases:
  - postgres-managed
name: PostgreSQL managed service
description: PostgreSQL database server managed by a Bus PostgreSQL provider.
service_kind: database.postgres
runtime:
  kind: managed
  provider: bus-postgres
  ports:
    - name: postgres
      protocol: tcp
      target: 5432
